diff --git modules/database/src/std/rec/histogramRecord.c modules/database/src/std/rec/histogramRecord.c
index 9f35870..55316e8 100644
--- modules/database/src/std/rec/histogramRecord.c
+++ modules/database/src/std/rec/histogramRecord.c
@@ -338,7 +338,8 @@ static long add_count(histogramRecord *prec)
         return 0;
 
     temp = prec->sgnl - prec->llim;
-    for (i = 1; i <= prec->nelm; i++){
+    /* stop at nelm-1 so a rounded wdth can't push i past the last bin */
+    for (i = 1; i < prec->nelm; i++){
         if (temp <= (double) i * prec->wdth)
             break;
     }
